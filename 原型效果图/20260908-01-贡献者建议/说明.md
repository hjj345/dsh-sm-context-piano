# 贡献者建议视觉原型 · 第一轮

基于用户提供的 DSH Web 截图，使用 imagegen 内置图片生成工具制作，未修改插件代码。

- A：PR #2 角色配色，用户蓝色、最后可见助手输出绿色、中间输出灰色，保留原轨道位置。
- B：PR #2 配色加 PR #3 吸边，轨道向对话区左缘移动，悬停预览随轨道移动。
- C：Issue #1 仅关键点模式概念，隐藏中间输出键，仅保留用户与最终输出键；此模式尚未包含在两个 PR 中。

这些是 AI 生成的视觉示意，不是运行 PR 后的真实截图。琴键角色按示例分配；截图本身不能还原每条消息角色。生成可能改变正文细节、琴键数量与间距，不可用它验收逐像素位置或真实消息分组。

PR #3 的精确位置规则为 `max(16, min(flowLeft - 108, 24))` CSS px，基于对话区坐标，非整个浏览器左缘；图中只展示向左吸边的视觉方向。静态图不展示 160ms 过渡和减少动态效果设置。

PR #2 的 final 表示每轮最后可见输出，不能据此认为模型已结束生成；流式过程中最后一段仍可能变化。

本轮输出保留在独立日期目录，后续生成应使用新的轮次目录，避免覆盖。
